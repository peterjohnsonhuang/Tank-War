from visual import*
from math import*
def construction():
    return [box(length=40, height=20,width=1), box(length=30, height=15,width=1), cylinder(axis=30*vector(1.0,0.0,0.0),radius=4), sphere(radius=3)]

